dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
        maven(url = uri("https://www.jitpack.io"))
    }
}
rootProject.name = "Primer Application"
include(":app")
